/**
 * UI-Feature-Schalter. Backend/API/DB bleiben aktiv; nur die Anzeige steuern.
 * Zum Wiederaktivieren: jeweils auf `true` setzen.
 */
export const UI_FEATURES = {
  habits: false,
  breathRitualHistory: false,
} as const;
